{
  "name": "inkfinite-crdt-proof-js",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "cargo build --offline --manifest-path ../Cargo.toml",
    "test": "node --test test/*.test.mjs",
    "test:twice": "node scripts/run-twice.mjs",
    "benchmark": "node scripts/benchmark.mjs --write"
  },
  "dependencies": { "@automerge/automerge": "3.2.6" }
}
